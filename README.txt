Есть:
*таблица в бд ServiceItems,состоящая из Id,,Sum,Comment,Type(определяет доход или расход)
*добавление,редактирование,удаление элементов таблицы
*вывод элементов талицы с сортировкий по Type

Надо сделать:
*хорошенько почистить код,разобраться что,зачем,почему, все прокоментировать и рассортировать
*выборку по дате добавления,сумме
*уйти от EntityFramework(чтобы можно было запускать без помощи сторонних фреймворков)

Для связи с бд я использовал EntityFramework, поэтому перед запуском необходимо создать таблицы(Если EntityFramework не установлен,то предварительно dotnet tool install -g dotnet-ef После dotnet ef database update).

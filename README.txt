Для связи с бд я использовал EntityFramework, поэтому перед запуском необходимо создать таблицы(dotnet ef database update. если EntityFramework не установлен,то предварительно dotnet tool install -g dotnet-ef).
Надо сделать:
*хорошенько почистить код,разобраться что,зачем,почему, все прокоментировать и рассортировать
*выборку по дате добавления,сумме
*уйти от EntityFramework(чтобы можно было запускать без помощи сторонних фреймворков)

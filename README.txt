У меня не получилось сделать выборку по датам из-за нехватки знаний, а в интернете я находил лишь обрывочную или устаревшую информацию, хотя представляю, как это реализовать(добавить в Work.Domain.Repositories.EntityFramework функцию c запросом в бд по условию(select * form ServiceItem where DateAdded ...),в контроллер и кнопку на index, как с созданием) 
Для связи с бд я использовал EntityFramework, поэтому перед запуском необходимо создать таблицы(dotnet ef database update. если EntityFramework не установлен,то предварительно dotnet tool install -g dotnet-ef).
